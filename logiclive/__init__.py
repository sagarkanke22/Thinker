"""LogicLive — Server-Driven UI platform for internal tools.

Architecture: declarative UI config + DB-resident Python business logic,
editable by hand or via a scoped AI chat. Frontend is a generic
config-renderer; backend executes per-widget logic in a sandbox and
returns config-with-data.
"""

__version__ = "0.1.0"
