Terminal 1 — Backend (FastAPI)
  cd /home/sagar123/Thinker
  source .venv/bin/activate
  uvicorn logiclive.app:app --reload --port 8000
  
  Terminal 2 — Frontend (Vite dev server)
  cd /home/sagar123/Thinker/frontend
  npm run dev
  
  Then open:
  - App (end-user view): http://localhost:5173/app?screen=sales_report
  - Editor (dev view): http://localhost:5173/editor
  - Plan (AI planner): http://localhost:5173/plan
  - API health check: http://localhost:8000/health

  Prerequisites (if first run):
  cd /home/sagar123/Thinker
  pip install -e .           # install logiclive package
  cd frontend
  npm install                # install JS deps